#include <stdio.h>
/**
 * main - prints a string to stdout
 * return - exits the program with value 0
 */
int main(void)
{
        puts("\"Programming is like building a multilingual puzzle");
        return (0);
}
