#include <stdio.h>
/**
 * main - print all single digit numbers of base 10 starting from 0
 * Return: 0 for success
 */
int main(void)
{
	int i;

	for ((char)i = '0'; (char)i < '10'; (char)i++)
	{
		putchar((char)i);
	}
	putchar('\n');
	return (0);
}
