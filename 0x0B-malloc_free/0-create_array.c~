#include <stdio.h>
#include "main.h"

char *create_array(unsigned int size, char c)
{
	char *ptr;
	int i;

	ptr = malloc(sizeof(char) * size);

	if (ptr)
	{
		for (i = 0; i < size; i++)
		{
			ptr[i] = c;
		}

		return (ptr);
	}
}
