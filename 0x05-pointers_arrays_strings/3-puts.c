#include "main.h"

/**
 * _puts - prints a string to std out
 * @s: pointer to base index of the string
 */

void _puts(char *str)
{
	int i;

	for (i = 0; *s != '\0'; i++)
	{
		_putchar(*s);
		s++;
	}

}
